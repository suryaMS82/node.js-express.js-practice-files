<%- include('./partials/header.ejs') %>
<body>

<%- include('./partials/nav.ejs') %>
    <div class="blogs content">
        <h2 style="font-size: 30px;">All blogs</h2>
    </div>
    <%# hey it just commenting tag #%>
    <% if(blogs.length>0){ %>
        <% blogs.forEach((blog)=> { %>
        <a class="single" href="/blogs/<%= blog._id %>">
           <h2><%= blog.title %></h2>
           <p><%= blog.snippet %></p>
        </a>
       <% }) %>
        <% } %>
</body>
</html>